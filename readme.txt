npm init -y
npm install express
npm install mysql2
npm install dotenv
npm install cors 

Crear archivo .env en la raiz con 
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_NAME=nombre_bd
DB_PORT=nro_puerto

Para ejecutar:
node src/index.js